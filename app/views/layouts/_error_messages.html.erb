<% if model.errors.any? %>
	<div class="alert alert-warning">
		<ul>
			<% model.form.object.errors.full_messages.each do |message| %>
			<li><%= message %></li>
			<% end %>
		</ul>
	</div>
<% end %>
